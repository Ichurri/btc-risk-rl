# Plan H3: adopción ADR-002 v2.1

Autorizado por el usuario; base e4e2e8f, rama codex/adopt-adr002-v2-1.
Conservar eliminación local de .python-version. Sin agentes, entrenamientos ni test final.

1. Registrar adopción y parámetros pendientes, sin modificar propuestas.
2. Escribir y ejecutar pruebas rojas de reloj, terminalidad, cortes y compatibilidad.
3. Versionar Config (schema 2) y TradingEnv (observación 13, objetivo finito).
   Conservar env/accounting.py, env/market.py y productos H1. Añadir puente
   estricto de configuración histórica en auditoría segmentada.
4. Adaptar pruebas H2 solo en interfaz temporal y auditor offline. Añadir
   fragmentación/continuación, rechazo temprano y validación sin reset.
5. Ejecutar Ruff y pytest; revisión. Ejecutar auditor con datos de desarrollo
   aceptados y acciones prefijadas; contrastar contabilidad con H2.
6. Versionar informe, resumen, comandos, logs y huellas pequeños. Actualizar
   README y principio de HANDOFF conservando historia. Commit y push sin force.

Comandos con UV_CACHE_DIR=/tmp/btc-risk-rl-uv-cache:
uv run --frozen pytest tests/test_temporal_contract.py (RED/GREEN)
uv run --frozen ruff check .
uv run --frozen pytest
uv run --frozen python scripts/verify_simulator.py --context local --output artifacts/simulator-h3/real

Distinguir pruebas nuevas del simulador de cálculos de propuestas anteriores.
